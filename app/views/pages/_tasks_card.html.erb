<%# task %>

<div class="card-task">
  <%= task.title %>
</div>

<%# Delete button %>

<%= link_to "Delete", task_path(task), method: :delete, class: "btn btn-primary" %>

<%# Edit button %>

<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal-<%= task.id%>" data-whatever="@mdo">
  Edit Post
</button>
<%= render "tasks/edit_modal", task: task %>

<%# Enter Work session %>

<% if task.work_session != nil %>
  <%= link_to work_session_path(task.work_session) do %>
  <% end %>
<% else %>
  <%= link_to task_work_sessions_path(task), method: :post do %>
  <% end %>
<% end %>
